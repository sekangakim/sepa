% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{boot_cis_all}
\alias{boot_cis_all}
\title{BCa (with percentile fallback) confidence intervals for all bootstrap indices}
\usage{
boot_cis_all(boot_obj, type = c("bca", "perc"), level = 0.95, idx_vec = NULL)
}
\arguments{
\item{boot_obj}{An object of class \code{"boot"} returned by
\code{\link[boot]{boot}}.}

\item{type}{Character vector passed to \code{boot.ci}'s \code{type}
argument.  Default \code{c("bca", "perc")}.}

\item{level}{Numeric confidence level.  Default \code{0.95}.}

\item{idx_vec}{Integer vector of column indices to process.  Defaults to
all columns of \code{boot_obj$t}.}
}
\value{
A data frame with columns \code{index}, \code{lwr}, \code{upr},
  and \code{method} (one row per element of \code{idx_vec}).
}
\description{
Loops over columns of a \code{boot} object and calls
\code{\link[boot]{boot.ci}} for each, returning a tidy data frame.  Falls
back to percentile intervals if the BCa calculation fails.
}
\examples{
\dontrun{
# See run_sepa() for an end-to-end example
}

}
