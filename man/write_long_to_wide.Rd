% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{write_long_to_wide}
\alias{write_long_to_wide}
\title{Reshape a long data frame to wide and write a CSV}
\usage{
write_long_to_wide(df, id_col, time_col, value_col, file, prefix = "")
}
\arguments{
\item{df}{Data frame to pivot.}

\item{id_col}{Name of the person-identifier column.}

\item{time_col}{Name of the within-person variable column (e.g. domain).}

\item{value_col}{Name of the value column.}

\item{file}{Character path for the output CSV.  Pass \code{NULL} or
\code{""} to skip writing.}

\item{prefix}{Optional prefix prepended to the new wide-format column
names (empty string = no prefix).}
}
\value{
The wide data frame, invisibly.
}
\description{
Pivots a three-column long data frame (id, time, value) to wide format and
optionally prefixes the new column names.
}
\examples{
long_df <- data.frame(
  id     = rep(1:3, each = 2),
  domain = rep(c("LT", "ST"), 3),
  value  = c(100, 105, 98, 110, 102, 107)
)
wide <- write_long_to_wide(long_df, "id", "domain", "value",
                           file = NULL)
wide

}
